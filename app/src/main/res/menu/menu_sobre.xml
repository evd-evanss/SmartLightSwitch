<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:title="Smart"
        android:titleCondensed="Smart"
        android:id="@+id/smart"
        android:enabled="true"
        android:icon="@drawable/ic_splash"
        app:showAsAction="ifRoom"/>

    <item
        android:title="Instalação"
        android:titleCondensed="Smart"
        android:id="@+id/settng"
        android:enabled="true"
        />

    <item
        android:title="Sobre"
        android:titleCondensed="Controle"
        android:id="@+id/info"
        android:enabled="true"
        />

</menu>